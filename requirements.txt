# Dependências do TCC — Previsão de EMC em PCBs com modelos informados por física.
# Instalar em ambiente virtual:  python3 -m venv venv && ./venv/bin/pip install -r requirements.txt
# Versões mínimas fixadas nas já validadas no venv atual (2026-09); demais sem pino
# até o primeiro experimento reproduzível em experiments/.

# --- base numérica e dados ---------------------------------------------------
numpy>=2.5
pandas>=3.0
scipy>=1.18
scikit-rf                # leitura Touchstone (.sNp) e conversão S↔Z
matplotlib>=3.11

# --- modelos de referência --------------------------------------------------
scikit-learn>=1.9        # regressão regularizada, floresta aleatória, GPR
lightgbm                 # gradient boosting

# --- modelo informado por física --------------------------------------------
torch                    # PINN: perda com termos físicos R1/R3/R4

# --- otimização --------------------------------------------------------------
optuna                   # pesos dos termos físicos (otimização bayesiana)
pymoo                    # NSGA-II / evolução diferencial multiobjetivo

# --- interpretabilidade ------------------------------------------------------
shap
pysr                     # regressão simbólica (requer Julia; ver docs do PySR)

# --- registro, teste e configuração -----------------------------------------
mlflow
pytest>=9.1
hydra-core               # configs em config/*.yaml (regra do CLAUDE.md)
